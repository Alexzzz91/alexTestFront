import React from 'react'
import CompaniesList from './CompaniesList/CompaniesList'
import PropTypes from 'prop-types'

const CompaniesPage = (props) => ( <CompaniesList/> );

CompaniesPage.propTypes = {
};

export default CompaniesPage
